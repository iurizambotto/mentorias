# Sessão 08 — Subir no Ubuntu

```bash
docker compose up -d
docker compose ps
```

## Acessos
- OpenMetadata: `http://localhost:8585`
- Airflow Ingestion do OpenMetadata: `http://localhost:8088`
- MongoDB: `localhost:27017`
