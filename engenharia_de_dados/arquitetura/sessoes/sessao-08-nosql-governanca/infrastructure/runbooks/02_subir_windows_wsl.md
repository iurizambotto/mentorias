# Sessão 08 — Subir no Windows + Docker Desktop + WSL

```bash
cd ~/zambotto_new/projects/zambotto-mentoria/docs/mentees/aurelius/sessions/sessao-08-nosql-governanca/infrastructure
docker compose up -d
docker compose ps
```

## Acesso no navegador do Windows
- OpenMetadata: `http://localhost:8585`
- Ingestion Airflow: `http://localhost:8088`
